# Lab API Server — Environment Variables
# Auto-generated by setup script — do not commit to git.

# Port the API server listens on (must match LAB_API_PORT in webapp/.env.local)
PORT=4100

# Absolute path to the folder containing docker-compose.yml
# Leave blank — server.js auto-detects using the folder one level above server.js.
LAB_DIR=
