{
  "name": "p2_sec_application",
  "version": "1.0.0",
  "description": "Pentest Command Center root startup scripts",
  "scripts": {
    "install:all": "cd lab-api && npm install && cd ../webapp && npm install",
    "start": "concurrently \"npm run start:api\" \"npm run start:web\"",
    "start:api": "cd lab-api && node server.js",
    "start:web": "cd webapp && npm run dev"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
